import React from 'react';

function Settings(props) {
    return (
        <div style={{width: '100vw', height: '80vh', textAlign: "center", paddingTop: 100}}>
            Settings
        </div>
    );
}

export default Settings;
